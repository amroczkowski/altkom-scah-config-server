spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/amroczkowski/altkom-scah-configuration-docker
          clone-on-start: true
